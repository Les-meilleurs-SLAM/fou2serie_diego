{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

<div class="text-center">
    <h1>Genres</h1>
    <br/><br/>
    <hr/>
    {% for unGenre in lesGenres %}
        <h4><a href="{{ path('infoGenre', {'id': unGenre.id}) }}" > {{ unGenre.libelle }}</a></h4>
        <br/>
        <hr/>
    {% endfor %}
</div>

{% endblock %}